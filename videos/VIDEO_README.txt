File: rocket-launch.mp4

The home page launch strip uses a static image: images/rocket-launch.png (full-width
object-fit: cover). This folder is optional; you can remove the MP4 if you do not
plan to use video elsewhere.

If you reintroduce video later, export at 1920×1080 or at least 1280×720, H.264 + AAC,
and tune object-position in CSS for your crop.
