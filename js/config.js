/**
 * AK Rocketry — site configuration
 *
 * CHMS outreach (outreach.html only)
 * - Google Form → Send → embed → copy iframe src as chmsFormEmbedUrl (?embedded=true).
 * - Optional chmsFormDirectUrl for “open in new tab.”
 *
 * Phase 3 SEO: replace __AK_ROCKETRY_SITE_URL__ in index.html and sitemap.xml after deploy.
 * Main club comms: Band (home page #join).
 */
window.AK_CONFIG = {
  chmsFormEmbedUrl: "",
  chmsFormDirectUrl: "",
};
